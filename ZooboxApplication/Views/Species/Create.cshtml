@model ZooboxApplication.Models.Specie

@{
    ViewData["Title"] = "Criar";
}

<h2>Criar <a href="#" data-toggle="tooltip" data-placement="bottom" title="Formulário para adicionar uma nova especie, somente preencher e submter o formulário"><i class="fa fa-question-circle" aria-hidden="true"></i></a></h2>

<h4>Espécie animal</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SpecieName" class="control-label"></label>
                <input asp-for="SpecieName" class="form-control" />
                <span asp-validation-for="SpecieName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" id="submitSpecie" value="Criar" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
