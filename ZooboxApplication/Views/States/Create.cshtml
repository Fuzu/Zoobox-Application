@model ZooboxApplication.Models.State

@{
    ViewData["Title"] = "Criar Estado do animal";
}

<h2>Criar Novo  <a href="#" data-toggle="tooltip" data-placement="bottom" title="Formulário para adicionar um novo estado, somente preencher e submter o formulário"><i class="fa fa-question-circle" aria-hidden="true"></i></a></h2>

<h4>Estado</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StateName" class="control-label"></label>
                <input asp-for="StateName" class="form-control" />
                <span asp-validation-for="StateName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Criar novo" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
